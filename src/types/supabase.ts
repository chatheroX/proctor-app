
// This file is a placeholder for your Supabase types.
// You can generate them using the Supabase CLI:
// supabase gen types typescript --project-id <your-project-id> --schema public > src/types/supabase.ts
// For now, we'll define a basic Json type and an empty Database interface.

export type Json =
  | string
  | number
  | boolean
  | null
  | { [key: string]: Json | undefined }
  | Json[];

export interface Database {
  public: {
    Tables: {
      proctorX: { 
        Row: {
          id: string; // uuid
          pass: string | null; // text
          created_at?: string; // Assuming a created_at, common in Supabase
        };
        Insert: {
          id?: string; // uuid is auto-generated by default
          pass: string | null;
          created_at?: string; 
        };
        Update: {
          id?: string;
          pass?: string | null;
          created_at?: string;
        };
      };
    };
    Views: {
      [_ in never]: never;
    };
    Functions: {
      [_ in never]: never;
    };
    Enums: {
      [_ in never]: never;
    };
    CompositeTypes: {
      [_ in never]: never;
    };
  };
}

// You can add specific user metadata types if you know them
export interface UserMetadata {
  full_name?: string;
  avatar_url?: string;
  role?: 'student' | 'teacher';
  [key: string]: any; // Allow other properties
}

